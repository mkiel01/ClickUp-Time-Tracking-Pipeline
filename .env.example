CLICKUP_API_KEY=replace_with_clickup_api_key
CLICKUP_TEAM_ID=replace_with_clickup_team_id

# Docker Streamlit container → host Mac Postgres:
POSTGRES_HOST=host.docker.internal
# Mac venv / python tracked_time_update.py on the host (same machine as Postgres):
# POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=clickup
POSTGRES_USER=mkiel
POSTGRES_PASSWORD=

# Set to 1 to skip pg_dump during full fetch (e.g. no client tools locally)
# SKIP_PG_DUMP=0

# GOOGLE_DRIVE_FOLDER_ID=1We1WUYqriSpew672xGV-CBkFZK5CgIEB
# GOOGLE_DRIVE_FILE_ID=your-spreadsheet-id   # pipeline CLI only (python drive_upload.py)
# SYNC_DAYS_BACK=2

WEBHOOK_POSTGRES_HOST=host.docker.internal
WEBHOOK_POSTGRES_PORT=5432
WEBHOOK_POSTGRES_DB=clickup_webhooks
WEBHOOK_POSTGRES_USER=clickup_user
WEBHOOK_POSTGRES_PASSWORD=
